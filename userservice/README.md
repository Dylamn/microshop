# User-service

## Configuration

The service is configured using environment variables.

Start by copying the `.env.example` file to `.env` and adjust the values as needed.

> To generate the `SECRET_KEY`, you can run `openssl rand -hex 32` in your terminal.